<%= simple_form_for(@item_datum) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :data %>
    <%= f.association :item %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
